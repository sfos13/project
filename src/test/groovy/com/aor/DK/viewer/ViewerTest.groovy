package com.aor.DK.viewer
/*class ViewerTest extends Specification{
    def 'check if drawElements is called'() {
        given:
            Arena arena = Mock()
        when:
            Arena.close()
        then:
            1 * viewer.draw(Mario mario(2,3),gui)
    }
}*/
